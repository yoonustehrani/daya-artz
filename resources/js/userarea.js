import React from 'react'
import ReactDOM from 'react-dom'

const elem = document.getElementById('main')

if (elem) {
    ReactDOM.render(
        <div>
            <h3>Hello Yoonus</h3>
        </div>,
        elem
    )
}